#ifndef ERROR_HPP
# define ERROR_HPP

# define SOCKET_FAILED "socket() failed."
# define SETSOCKOPT_FAILED "setsockopt() failed"
# define INET_ADDR_FAILED "inet_addr(), malformed requests."
# define BIND_FAILED "bind() failed."
# define LISTEN_FAILED "listen() failed."
# define GPBN_FAIL "getprotobyname() failed."

#endif
